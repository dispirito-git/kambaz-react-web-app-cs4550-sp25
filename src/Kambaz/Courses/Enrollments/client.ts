
/*import axios from "axios";
const REMOTE_SERVER = import.meta.env.VITE_REMOTE_SERVER;
const ENROLLMENTS_API = `${REMOTE_SERVER}/api/enrollments`;
export const findMyEnrollments = async () => {
  const { data } = await axios.get(`${ENROLLMENTS_API}/current`);
  return data;
};
export const createEnrollment = async (enrollment: any) => {
    const { data } = await axios.post(ENROLLMENTS_API, enrollment);
    return data;
}
export const deleteEnrollment = async (enrollmentId: string) => {
    const { data } = await axios.delete(`${ENROLLMENTS_API}/${enrollmentId}`);
    return data;
}
*/